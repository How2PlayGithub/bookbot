# BookBot

Features List:

- Book Search
- Book Length
- Character Count
- Word Search

To use:
Run `mkdir books` within the directory
Add the book (in a .txt format) into `./books/`
Run `python3 main.py`
